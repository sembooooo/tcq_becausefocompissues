name: Build and run

on: [push, pull_request]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: build
      run: gcc -DMAIN -Wall -Wextra -o executer ./Sender/src/*.c
    - name: run
      run: ./executer 